<div class="container">
  <div class="row">

<h2>商品一覧</h2>

<% @items.each do |item| %>
  <div class="col-md-3">
  <%= image_tag item.image, size: "80x50" %>
  <%= item.name %>
  <%= (item.price*1.1).floor %>
  </div>
<% end %>

<%= paginate @items %>    
  </div>
</div>
